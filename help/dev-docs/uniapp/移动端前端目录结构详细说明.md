# CRMEB 移动端前端目录结构详细说明

## 📋 项目概览

**技术栈**: Vue.js 2.x + uni-app + Vuex + SCSS  
**多端支持**: H5、微信小程序、支付宝小程序、百度小程序、头条小程序、App  
**项目版本**: CRMEB标准版 5.6.2  
**开发模式**: 组件化 + 模块化 + 分包加载

## 🏗️ 整体架构设计3

```
template/uni-app/
├── 📁 api/                    # 接口服务层 (120+ API接口)
├── 📁 components/             # 组件层 (60+ 可复用组件)
├── 📁 config/                 # 配置层
├── 📁 libs/                   # 工具库层
├── 📁 mixins/                 # 混入层
├── 📁 pages/                  # 页面层 (分包架构)
├── 📁 plugin/                 # 插件层
├── 📁 static/                 # 静态资源层
├── 📁 store/                  # 状态管理层
├── 📁 utils/                  # 工具函数层
├── 🚀 App.vue                 # 应用根组件
├── 🎯 main.js                 # 应用入口
├── ⚙️ manifest.json           # 应用配置文件
├── 📄 pages.json              # 页面路由配置
└── 📦 package.json            # 依赖配置
```

## 📊 详细目录结构解析

### 1. 📁 api/ - 接口服务层

按业务模块划分的API接口管理，采用Promise封装：

```javascript
// 文件结构
api/
├── activity.js      # 营销活动接口 (砍价、拼团、秒杀等)
├── admin.js         # 管理端接口
├── api.js           # 基础API接口
├── kefu.js          # 客服系统接口
├── lottery.js       # 抽奖活动接口  
├── order.js         # 订单管理接口
├── points_mall.js   # 积分商城接口
├── public.js        # 公共接口 (配置获取等)
├── store.js         # 门店相关接口
├── user.js          # 用户中心接口
```

**接口设计特点**:
- 统一的错误处理机制
- 请求参数验证
- 响应数据格式化
- 支持多端环境适配

### 2. 📁 components/ - 组件层

丰富的组件库，采用原子化设计思想：

#### 2.1 基础组件
- `Authorize.vue` - 用户授权组件
- `BaseMoney.vue` - 金额显示组件（支持格式化）
- `BaseTag.vue` - 标签组件（多种样式）
- `emptyPage.vue` - 空状态页面组件
- `menuIcon.vue` - 菜单图标组件

#### 2.2 业务组件
- `addressWindow/` - 地址选择组件（三级联动）
- `cartList/` - 购物车商品列表
- `countDown/` - 倒计时组件（多种样式）
- `couponWindow/` - 优惠券选择组件
- `goodList/` - 商品列表组件（网格/列表布局）
- `home/` - 首页业务组件集合
- `orderGoods/` - 订单商品组件
- `payment/` - 支付组件（多支付方式）

#### 2.3 功能组件
- `easy-loadimage/` - 图片懒加载组件
- `jyf-parser/` - 富文本解析组件（支持HTML/Markdown）
- `Loading/` - 加载状态组件
- `skeleton/` - 骨架屏组件
- `swipers/` - 轮播图组件（可配置指示器）
- `update/` - 应用更新组件
- `WaterfallsFlow/` - 瀑布流布局组件

#### 2.4 特殊组件
- `parabolaBall/` - 抛物线动画组件（加入购物车动画）
- `shareRedPackets/` - 红包分享组件
- `tuiDrawer/` - 抽屉组件
- `uniNoticeBar/` - 通知栏组件

### 3. ?? config/ - 配置层

```javascript
config/
├── app.js       # 应用基础配置
│   - HTTP_REQUEST_URL    # API基础地址
│   - IMG_URL             # 图片资源地址  
│   - VERSION             # 应用版本
│   - WS_URL              # WebSocket地址
├── cache.js     # 缓存配置
│   - 缓存键名定义
│   - 缓存策略配置
└── socket.js    # Socket配置
```

### 4. 📁 libs/ - 工具库层

```javascript
libs/
├── chat.js          # 聊天功能核心库
├── login.js         # 登录认证工具
├── new_chat.js      # 新版聊天服务
├── permission.js    # 权限验证工具
├── routine.js       # 小程序相关工具
├── uniApi.js        # uni-app API增强
└── wechat.js        # 微信SDK封装
```

### 5. 📁 mixins/ - 混入层

```javascript
mixins/
├── color.js             # 颜色主题混入
├── debounce.js          # 防抖函数混入
├── SendVerifyCode.js    # 验证码发送混入
├── sharePoster.js       # 分享海报生成混入
└── skuSelect.js         # 商品SKU选择混入
```

### 6. 📁 pages/ - 页面层（分包架构）

#### 6.1 主包页面（核心页面）
- `guide/index` - 应用引导页
- `index/index` - 首页（TabBar）
- `order_addcart/order_addcart` - 购物车页（TabBar）
- `user/index` - 个人中心页（TabBar）
- `goods_cate/goods_cate` - 商品分类页（TabBar）

#### 6.2 商品模块分包 (goods)
```javascript
pages/goods/
├── goods_list/index             # 商品列表
├── goods_search/index           # 商品搜索
├── order_confirm/index          # 订单确认
├── order_details/index          # 订单详情
├── goods_details_store/index    # 门店列表
├── goods_comment_con/index      # 商品评价
├── cashier/index                # 收银台
└── lottery/grids/index          # 抽奖活动
```

#### 6.3 用户模块分包 (users)
```javascript
pages/users/
├── user_info/index              # 用户信息
├── user_address/index           # 地址管理
├── user_coupon/index            # 优惠券
├── user_spread_user/index       # 推广管理
├── user_money/index             # 我的账户
├── login/index                  # 登录页面
└── message_center/index         # 消息中心
```

#### 6.4 活动模块分包 (activity)
```javascript
pages/activity/
├── goods_bargain/index          # 砍价活动
├── goods_combination/index      # 拼团活动  
├── goods_seckill/index          # 秒杀活动
├── presell/index                # 预售活动
└── bargain/index                # 砍价记录
```

#### 6.5 管理模块分包 (admin)
```javascript
pages/admin/
├── order/index                  # 订单管理
├── orderList/index              # 订单列表
├── statistics/index             # 数据统计
├── delivery/index               # 订单发货
└── order_cancellation/index     # 订单核销
```

#### 6.6 其他分包
- `annex/` - 附件页面（Webview、会员等）
- `points_mall/` - 积分商城
- `extension/` - 扩展功能（客服、资讯等）
- `columnGoods/` - 专栏商品
- `goods_details/` - 商品详情（独立分包）

### 7. 📁 static/ - 静态资源层

#### 7.1 样式资源
```css
static/
├── css/
│   ├── base.css         # 基础样式重置
│   ├── style.scss       # 主要样式文件
│   ├── unocss.css       # UnoCSS工具样式
│   └── guildford.css    # 特定样式
├── fonts/               # 字体文件
│   └── D-DIN-PRO-*.ttf
└── iconfont/            # 图标字体
    └── iconfont.css
```

#### 7.2 图片资源
```bash
static/images/
├── 1-001.png ~ 4-002.png    # TabBar图标
├── def_avatar.png           # 默认头像
├── vip.png                  # VIP标识
├── svip.png                 # SVIP标识  
└── 各种功能图标和背景图片
```

#### 7.3 其他资源
- `app-plus/` - App特有资源
- `easy-loadimage/` - 图片加载相关资源

### 8. 📁 store/ - 状态管理层

Vuex模块化状态管理：

```javascript
store/
├── modules/            # 状态模块
│   ├── app.js         # 应用状态
│   ├── hotWords.js    # 搜索热词状态
│   └── indexData.js   # 首页数据状态
├── getters.js         # Getter函数
└── index.js           # Store主入口
```

**状态管理特点**:
- 模块化状态划分
- 严格的mutations修改
- 支持状态持久化
- 多端状态同步

### 9. 📁 utils/ - 工具函数层

```javascript
utils/
├── cache.js          # 缓存管理工具
├── emoji.js          # 表情处理工具
├── index.js          # 工具主入口
├── lang.js           # 多语言工具
├── permission.js     # 权限验证工具
├── request.js        # 请求封装工具
├── util.js           # 通用工具函数
├── validate.js       # 数据验证工具
└── theme.js          # 主题管理工具
```

## 🚀 核心技术特性

### 1. 多端适配机制
```javascript
// 条件编译示例
// #ifdef H5
// H5特有代码
// #endif

// #ifdef MP-WEIXIN  
// 微信小程序特有代码
// #endif

// #ifdef APP-PLUS
// App特有代码
// #endif
```

### 2. 性能优化策略
- **分包加载**: 按业务模块分包，减少主包体积
- **图片懒加载**: 滚动可视区域加载
- **组件懒加载**: 路由级别的代码分割
- **数据缓存**: 合理的缓存策略
- **请求防抖**: 避免重复请求

### 3. 主题换肤系统
支持动态主题色切换，通过CSS变量实现：
```css
:root {
  --view-theme: #e93323; /* 主要主题色 */
  --view-assist: #ff9900; /* 辅助色 */
}
```

### 4. 国际化支持
基于Vue I18n的多语言支持，支持动态语言包加载。

## 📋 开发规范

### 1. 命名规范
- **目录命名**: 小写字母，使用连字符 `my-component`
- **文件命名**: Vue组件使用帕斯卡命名 `MyComponent.vue`
- **变量命名**: 驼峰命名 `userInfo`
- **常量命名**: 全大写加下划线 `API_BASE_URL`

### 2. 代码组织
- 单一职责原则
- 组件props验证
- 错误边界处理
- 合理的注释规范

### 3. 提交规范
- feat: 新功能
- fix: 修复bug
- docs: 文档更新
- style: 代码格式
- refactor: 代码重构

## 🛠️ 开发命令

```bash
# 开发环境
npm run dev:[platform]

# 生产构建  
npm run build:[platform]

# 代码检查
npm run lint

# 示例
npm run dev:mp-weixin    # 微信小程序开发
npm run build:h5         # H5生产构建
npm run build:app-plus   # App生产构建
```

## 📝 注意事项

1. **环境配置**: 确保正确配置API地址和第三方服务密钥
2. **平台差异**: 注意不同平台的API兼容性
3. **图片资源**: 使用相对路径，注意多端适配
4. **权限申请**: 按需申请平台权限
5. **版本兼容**: 注意uni-app版本兼容性

---

*本文档基于CRMEB 5.6.2版本分析生成，最后更新: 2026-01-19*  
*📧 如有疑问请联系开发团队: admin@crmeb.com*

---

> **提示**：该文档由AI生成，仅供参考。
