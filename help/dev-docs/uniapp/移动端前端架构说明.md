# CRMEB 移动端前端架构说明

## 📱 技术栈概览

| 技术 | 版本 | 说明 |
|------|------|------|
| UniApp | 2.x/3.x | 跨端开发框架 |
| Vue.js | 2.x/3.x | 核心框架 |
| Vuex | 3.x/4.x | 状态管理 |
| uView UI | 2.x | 移动端UI组件库 |
| Sass/Less | CSS预处理器 | |
| Axios | HTTP客户端 | 封装请求 |

## 🏗️ 整体架构

```
┌─────────────────────────────────────────────────────────────┐
│                      UniApp 框架层                           │
│                    (跨端编译引擎)                            │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│                      Vue.js 核心层                           │
│                    (响应式数据驱动)                          │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│                      状态管理层                              │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐       │
│  │ User模块 │ │ Cart模块 │ │ Order模块│ │ Config模块│       │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘       │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│                      UI 组件层                               │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  基础组件   │  业务组件   │  自定义组件   │            │
│  └─────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│                      多端适配层                              │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐       │
│  │ 微信小程序 │ │  H5页面  │ │  APP端   │ │ 其他平台  │       │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘       │
└─────────────────────────────────────────────────────────────┘
```

## 🧩 核心功能模块

### 1. 商品模块
- 商品列表展示
- 商品详情页
- 商品搜索
- 商品分类

### 2. 购物车模块
- 添加商品到购物车
- 购物车管理
- 批量操作

### 3. 订单模块
- 订单创建
- 订单列表
- 订单详情
- 订单状态跟踪

### 4. 用户模块
- 用户登录注册
- 个人中心
- 地址管理
- 收藏管理

### 5. 支付模块
- 微信支付
- 支付宝支付
- 余额支付

## 🔧 开发最佳实践

### 1. 页面生命周期
```javascript
export default {
  // 页面加载
  onLoad(options) {
    this.initPage(options)
  },

  // 页面显示
  onShow() {
    this.refreshData()
  },

  // 下拉刷新
  onPullDownRefresh() {
    this.loadData().finally(() => {
      uni.stopPullDownRefresh()
    })
  },

  // 上拉加载
  onReachBottom() {
    this.loadMore()
  },

  methods: {
    async initPage(options) {
      // 初始化页面数据
    },

    async refreshData() {
      // 刷新数据
    }
  }
}
```

### 2. 组件通信
```javascript
// 父传子
<child-component :data="parentData" @custom-event="handleEvent" />

// 子组件
export default {
  props: {
    data: {
      type: Object,
      default: () => ({})
    }
  },
  methods: {
    emitEvent() {
      this.$emit('custom-event', eventData)
    }
  }
}
```

### 3. 状态管理
```javascript
// 使用 Vuex
import { mapState, mapActions } from 'vuex'

export default {
  computed: {
    ...mapState('user', ['userInfo']),
    ...mapState('cart', ['cartList'])
  },
  methods: {
    ...mapActions('user', ['login']),
    ...mapActions('cart', ['addToCart'])
  }
}
```

## 🎯 性能优化

### 1. 图片优化
```javascript
// 使用懒加载
<image lazy-load src="image.jpg" />

// 使用webp格式
<image webp src="image.webp" />

// 使用合适的图片尺寸
<image :src="imageUrl + '?x-oss-process=image/resize,w_300'" />
```

### 2. 数据缓存
```javascript
// 使用本地存储
uni.setStorageSync('key', data)
const data = uni.getStorageSync('key')

// 使用内存缓存
const cache = new Map()
cache.set('key', data)
const data = cache.get('key')
```

### 3. 请求优化
```javascript
// 防抖处理
const debounce = (func, wait) => {
  let timeout
  return function() {
    clearTimeout(timeout)
    timeout = setTimeout(() => func.apply(this, arguments), wait)
  }
}

// 使用
search: debounce(function(keyword) {
  this.doSearch(keyword)
}, 300)
```

## 📦 多端适配

### 1. 条件编译
```javascript
// #ifdef H5
console.log('只在H5平台显示')
// #endif

// #ifdef MP-WEIXIN
console.log('只在微信小程序显示')
// #endif

// #ifdef APP-PLUS  
console.log('只在App显示')
// #endif
```

### 2. 平台差异处理
```javascript
const platform = {
  isH5: uni.getSystemInfoSync().platform === 'h5',
  isWeapp: uni.getSystemInfoSync().platform === 'devtools',
  isApp: uni.getSystemInfoSync().platform === 'app'
}

// 使用
if (platform.isH5) {
  // H5特有逻辑
}
```

## 🚀 部署发布

### 1. 小程序发布
```bash
# 微信小程序
npm run build:mp-weixin
# 然后使用微信开发者工具上传

# 支付宝小程序  
npm run build:mp-alipay
```

### 2. H5部署
```bash
# 构建H5
npm run build:h5

# 部署到服务器
scp -r dist/build/h5 user@server:/path/to/www
```

### 3. App发布
```bash
# 构建App
npm run build:app

# 使用HBuilderX进行打包和发布
```

## 🔍 调试技巧

### 1. 开发工具使用
```javascript
// 打印调试信息
console.log('调试信息', data)

// 使用debugger
debugger

// 性能监控
console.time('operation')
// 执行操作
console.timeEnd('operation')
```

### 2. 真机调试
```javascript
// 使用uni-app调试工具
// 1. 连接手机和电脑
// 2. 开启USB调试
// 3. 使用HBuilderX真机运行
```

## 📝 代码规范

### 1. 命名规范
```javascript
// 变量命名 - 小驼峰
const userName = 'John'
let isLoading = false

// 常量命名 - 全大写
const API_BASE_URL = 'https://api.example.com'
const MAX_COUNT = 100

// 组件命名 - 大驼峰
// MyComponent.vue
export default {
  name: 'MyComponent'
}
```

### 2. 代码组织
```javascript
// 文件结构
export default {
  // 1. 组件名
  name: 'ComponentName',

  // 2. 组件属性
  props: {},

  // 3. 数据
  data() {
    return {}
  },

  // 4. 计算属性
  computed: {},

  // 5. 监听器
  watch: {},

  // 6. 生命周期
  mounted() {},

  // 7. 方法
  methods: {}
}
```

---
**文档版本**: v1.0  
**更新日期**: 2024-01-17  
**适用版本**: CRMEB 5.6.4+  

💡 提示：移动端开发需要特别注意性能优化和多端适配，建议使用uni-app的Conditional Compile功能处理平台差异。

---

> **提示**：该文档由AI生成，仅供参考。
