# CRMEB 系统事件开发文档

## 1. 事件机制概述

CRMEB 后端系统采用**事件驱动架构**，通过事件机制实现模块间的解耦和通信。事件系统允许在不直接依赖的情况下，实现不同模块间的交互和业务逻辑扩展。

### 1.1 核心概念

- **事件(Event)**：系统中发生的特定动作或状态变化，如订单创建、用户注册等
- **监听器(Listener)**：监听特定事件并执行相应处理逻辑的类
- **事件触发(Trigger)**：在特定业务逻辑点调用事件，通知相关监听器执行

## 2. 事件系统架构

### 2.1 监听器接口定义

所有监听器必须实现 `crmeb\interfaces\ListenerInterface` 接口，该接口定义了统一的事件处理方法：

```php
<?php
namespace crmeb\interfaces;

interface ListenerInterface
{
    public function handle($event): void;
}
```

### 2.2 事件配置与注册

事件通过 `app/event.php` 文件进行定义和注册，该文件返回一个包含事件配置的数组：

```php
return [
    'bind' => [], // 事件绑定
    'listen' => [ // 事件监听
        'EventName' => [
            'Listener1::class',
            'Listener2::class',
        ],
    ],
];
```

**示例配置**：
```php
return [
    'listen' => [
        'HttpEnd' => [\app\listener\http\HttpEndListener::class], //HTTP请求结束回调事件
        'UserRegisterListener' => [\app\listener\user\RegisterListener::class], //用户注册后置事件
        'OrderCreateAfterListener' => [\app\listener\order\OrderCreateAfterListener::class], //订单创建后置事件
        // 更多事件...
    ],
];
```

## 3. 系统内置事件

CRMEB 系统内置了丰富的事件，涵盖了系统运行的各个方面：

| 事件类别 | 事件名称 | 触发时机 | 监听器类 |
|---------|---------|---------|---------|
| **基础事件** | HttpEnd | HTTP请求结束 | \app\listener\http\HttpEndListener::class |
| | QueueStartListener | 队列启动 | \app\listener\queue\QueueStartListener::class |
| **用户相关** | UserLoginListener | 用户登录 | \app\listener\user\LoginListener::class |
| | AdminLoginListener | 管理员登录 | \app\listener\admin\AdminLoginListener::class |
| | UserRegisterListener | 用户注册 | \app\listener\user\RegisterListener::class |
| | WechatAuthListener | 微信授权 | \app\listener\wechat\AuthListener::class |
| | UserLevelListener | 用户升级 | \app\listener\user\UserLevelListener::class |
| | UserVisitListener | 用户访问 | \app\listener\user\UserVisitListener::class |
| **订单相关** | OrderCreateAfterListener | 订单创建 | \app\listener\order\OrderCreateAfterListener::class |
| | OrderPaySuccessListener | 订单支付成功 | \app\listener\order\OrderPaySuccessListener::class |
| | OrderDeliveryListener | 订单发货 | \app\listener\order\OrderDeliveryListener::class |
| | OrderTakeListener | 订单收货 | \app\listener\order\OrderTakeListener::class |
| | OrderRefundCreateAfterListener | 售后单生成 | \app\listener\order\OrderRefundCreateAfterListener::class |
| | OrderRefundCancelAfterListener | 售后单取消 | \app\listener\order\OrderRefundCancelAfterListener::class |
| **其他事件** | OutPushListener | 对外推送 | \app\listener\out\OutPushListener::class |
| | NoticeListener | 通知消息 | \app\listener\notice\NoticeListener::class |
| | CustomEventListener | 自定义事件 | \app\listener\CustomEventListener::class |

## 4. 事件触发与处理

### 4.1 事件触发方式

事件通过 `event()` 函数进行触发，该函数是 ThinkPHP 框架提供的全局函数：

```php
// 触发事件
event('EventName', $eventData);

// 示例：触发订单创建事件
event('OrderCreateAfterListener', [$order, $group, $uid, $key, $combinationId, $seckillId, $bargainId]);
```

### 4.2 事件处理流程

1. **事件触发**：在业务逻辑中通过 `event()` 函数触发特定事件
2. **事件分发**：系统根据事件名称查找对应的监听器列表
3. **监听器执行**：依次调用每个监听器的 `handle()` 方法，并传递事件数据
4. **业务处理**：监听器执行各自的业务逻辑，如数据处理、消息发送等
5. **处理完成**：所有监听器执行完成后，事件处理流程结束

## 5. 监听器实现

### 5.1 监听器创建步骤

1. **创建监听器类**：在 `app/listener/` 目录下创建相应的监听器类
2. **实现接口**：实现 `crmeb\interfaces\ListenerInterface` 接口
3. **编写处理逻辑**：在 `handle()` 方法中编写事件处理逻辑
4. **注册监听器**：在 `app/event.php` 文件中注册监听器

### 5.2 监听器实现示例

#### 5.2.1 订单创建事件监听器

```php
<?php
namespace app\listener\order;

use app\jobs\notice\PrintJob;
use app\jobs\ProductLogJob;
use app\jobs\UnpaidOrderCancelJob;
use app\jobs\UnpaidOrderSend;
use app\services\order\StoreOrderCreateServices;
use app\services\order\StoreOrderStatusServices;
use crmeb\interfaces\ListenerInterface;
use crmeb\services\CacheService;
use crmeb\services\SystemConfigService;
use crmeb\utils\Arr;

/**
 * 订单创建后置事件
 * Class OrderCreateAfterListener
 * @package app\listener\order
 */
class OrderCreateAfterListener implements ListenerInterface
{
    public function handle($event): void
    {
        [$order, $group, $uid, $key, $combinationId, $seckillId, $bargainId] = $event;

        // 订单数据创建之后的商品实际金额计算，佣金计算，优惠折扣计算，设置默认地址，清理购物车
        /** @var StoreOrderCreateServices $orderCreate */
        $orderCreate = app()->make(StoreOrderCreateServices::class);
        $orderCreate->orderCreateAfter($order, $group, $combinationId || $seckillId || $bargainId);

        // 清除订单缓存
        CacheService::delete('user_order_' . $uid . $key);

        // 写入订单记录表
        /** @var StoreOrderStatusServices $statusService */
        $statusService = app()->make(StoreOrderStatusServices::class);
        $statusService->save([
            'oid' => $order['id'],
            'change_type' => 'cache_key_create_order',
            'change_message' => '订单生成',
            'change_time' => time()
        ]);

        // 订单自动取消
        $this->pushJob($order['id'], $combinationId, $seckillId, $bargainId);

        // 下单记录
        ProductLogJob::dispatch(['order', ['uid' => $uid, 'order_id' => $order['id']]]);

        // 小票打印
        PrintJob::dispatch([$order['id'], 2]);
    }

    /**
     * 订单自动取消加入延迟消息队列
     * @param int $orderId
     * @param int $combinationId
     * @param int $seckillId
     * @param int $bargainId
     * @return mixed
     */
    public function pushJob(int $orderId, int $combinationId, int $seckillId, int $bargainId)
    {
        // 系统预设取消订单时间段
        $keyValue = ['order_cancel_time', 'order_activity_time', 'order_bargain_time', 'order_seckill_time', 'order_pink_time'];
        // 获取配置
        $systemValue = SystemConfigService::more($keyValue);
        // 格式化数据
        $systemValue = Arr::setValeTime($keyValue, is_array($systemValue) ? $systemValue : []);
        if ($combinationId) {
            $secs = $systemValue['order_pink_time'] ?: $systemValue['order_activity_time'];
        } elseif ($seckillId) {
            $secs = $systemValue['order_seckill_time'] ?: $systemValue['order_activity_time'];
        } elseif ($bargainId) {
            $secs = $systemValue['order_bargain_time'] ?: $systemValue['order_activity_time'];
        } else {
            $secs = $systemValue['order_cancel_time'];
        }
        // 未支付10分钟后发送短信
        UnpaidOrderSend::dispatchSecs(600, [$orderId]);
        // 未支付根据系统设置事件取消订单
        UnpaidOrderCancelJob::dispatchSecs((int)($secs * 3600), [$orderId]);
    }
}
```

#### 5.2.2 用户注册事件监听器

```php
<?php
namespace app\listener\user;

use app\jobs\AgentJob;
use app\services\activity\coupon\StoreCouponIssueServices;
use app\services\user\UserBillServices;
use app\services\user\UserServices;
use app\services\user\UserSpreadServices;
use crmeb\interfaces\ListenerInterface;

/**
 * 注册完成后置事件
 * Class RegisterListener
 * @package app\listener\user
 */
class RegisterListener implements ListenerInterface
{
    /**
     * 注册完成后置事件
     * @param $event
     * @throws \think\db\exception\DataNotFoundException
     * @throws \think\db\exception\DbException
     * @throws \think\db\exception\ModelNotFoundException
     */
    public function handle($event): void
    {
        [$spreadUid, $userType, $name, $uid, $isNew] = $event;

        if ($spreadUid) {
            if ($isNew) {
                // 邀请新用户增加经验
                /** @var UserBillServices $userBill */
                $userBill = app()->make(UserBillServices::class);
                $userBill->inviteUserIncExp((int)$spreadUid);
                // 增加推广佣金
                /** @var UserServices $userServices */
                $userServices = app()->make(UserServices::class);
                $userServices->addBrokeragePrice($uid, $spreadUid);

                // 推广新人 处理自己、上级分销等级升级
                AgentJob::dispatch([$uid]);
            }
            // 记录推广绑定关系
            /** @var UserSpreadServices $userSpreadServices */
            $userSpreadServices = app()->make(UserSpreadServices::class);
            $res = $userSpreadServices->setSpread($uid, $spreadUid);

            // 自定义消息-下级用户绑定成功
            if ($res) {
                $phone = app()->make(UserServices::class)->value($spreadUid, 'phone');
                event('CustomNoticeListener', [$spreadUid, ['nickname' => $name, 'time' => date('Y-m-d H:i:s'), 'phone' => $phone], 'spread_success']);
            }
        }

        if ($isNew) {
            // 新人优惠券发送
            /**@var StoreCouponIssueServices $storeCoupon */
            $storeCoupon = app()->make(StoreCouponIssueServices::class);
            $storeCoupon->userFirstSubGiveCoupon((int)$uid);

            // 人人分销开启推广权限
            if (sys_config('brokerage_func_status') && sys_config('store_brokerage_statu') == 2) {
                /** @var UserServices $userServices */
                $userServices = app()->make(UserServices::class);
                $userServices->update($uid, ['is_promoter' => 1]);
            }
        }
    }
}
```

## 6. 自定义事件

CRMEB 支持自定义事件，通过 `CustomEventListener` 实现灵活的业务扩展：

### 6.1 自定义事件触发

```php
// 触发自定义事件
event('CustomEventListener', ['event_mark', ['data1' => 'value1', 'data2' => 'value2']]);
```

### 6.2 自定义事件处理

自定义事件由 `app\listener\CustomEventListener` 处理，该监听器从数据库中获取配置的自定义代码并执行：

```php
<?php
namespace app\listener;

use app\services\system\SystemEventServices;
use think\facade\Log;

class CustomEventListener
{
    public function handle($event)
    {
        [$mark, $data] = $event;
        try {
            $list = app()->make(SystemEventServices::class)->selectList(['mark' => $mark, 'is_del' => 0, 'is_open' => 1])->toArray();
            foreach ($list as $item) {
                eval(json_decode($item['customCode']));
            }
        } catch (\Throwable $e) {
            $listener_log_open = config("log.listener_log", false);
            if ($listener_log_open) {
                $date = date('Y-m-d H:i:s', time());
                Log::write($date . '自定义事件错误:' . $e->getMessage(), 'listener');
            }
        }
    }
}
```

### 6.3 自定义事件配置

自定义事件可以通过后台管理系统进行配置，在 "系统设置" -> "事件管理" 中添加和管理自定义事件。

## 7. 最佳实践

### 7.1 事件命名规范

- 事件名称应清晰表达事件的含义和触发时机，如 `OrderCreateAfterListener` 表示订单创建后的事件
- 使用驼峰命名法，首字母大写

### 7.2 监听器设计原则

- **单一职责**：每个监听器应只负责处理一个特定的业务逻辑
- **无副作用**：监听器处理不应修改原始事件数据，避免影响其他监听器
- **异常处理**：在监听器中适当捕获异常，避免单个监听器失败影响整个事件处理流程

### 7.3 性能优化

- **异步处理**：对于耗时的操作，建议使用队列进行异步处理，如发送短信、生成报表等
- **缓存使用**：合理使用缓存减少数据库查询，提高处理效率
- **避免循环依赖**：监听器之间不应相互依赖，避免形成循环调用

## 8. 常见问题与解决方案

### 8.1 事件不触发

**问题**：调用 `event()` 函数后，监听器未执行。

**解决方案**：
- 检查事件名称是否与 `event.php` 中注册的名称一致
- 检查监听器类是否正确实现了 `ListenerInterface` 接口
- 检查监听器类是否正确注册到对应的事件中

### 8.2 监听器执行顺序

**问题**：需要控制监听器的执行顺序。

**解决方案**：
- 在 `event.php` 中，监听器的注册顺序决定了执行顺序
- 如需更复杂的控制，可以在监听器中手动触发其他事件

### 8.3 事件数据传递

**问题**：监听器无法获取完整的事件数据。

**解决方案**：
- 确保事件触发时传递了所有必要的数据
- 使用数组形式传递数据，便于扩展和维护
- 在监听器中对数据进行适当的验证和转换

## 9. 代码优化建议

### 9.1 监听器异常处理优化

**问题**：部分监听器可能缺少完善的异常处理机制，导致单个监听器失败影响整个事件处理流程。

**建议**：在监听器的 `handle()` 方法中添加 try-catch 块，捕获并记录异常，确保即使某个监听器失败，也不会影响其他监听器的执行。

```php
public function handle($event): void
{
    try {
        // 监听器逻辑
    } catch (\Exception $e) {
        // 记录异常日志
        Log::error('Listener error: ' . $e->getMessage(), [
            'listener' => __CLASS__,
            'event' => $event,
            'trace' => $e->getTraceAsString()
        ]);
        // 可以根据需要添加其他处理逻辑
    }
}
```

### 9.2 事件数据验证优化

**问题**：监听器对事件数据的依赖较强，但缺少对数据有效性的验证，可能导致运行时错误。

**建议**：在监听器中添加数据验证逻辑，确保接收到的数据符合预期格式和要求。

```php
public function handle($event): void
{
    // 验证事件数据
    if (!is_array($event) || count($event) < 4) {
        Log::warning('Invalid event data', [
            'listener' => __CLASS__,
            'event' => $event
        ]);
        return;
    }
    
    [$order, $group, $uid, $key] = $event;
    
    // 验证必要参数
    if (empty($order['id']) || empty($uid)) {
        Log::warning('Missing required parameters', [
            'listener' => __CLASS__,
            'order_id' => $order['id'] ?? null,
            'uid' => $uid
        ]);
        return;
    }
    
    // 监听器逻辑
}
```

### 9.3 事件触发点优化

**问题**：事件触发点可能过于分散，导致难以追踪事件的触发位置和时机。

**建议**：建立事件触发点的文档或注释规范，明确记录每个事件的触发位置、时机和传递的数据结构，便于后续维护和扩展。

```php
/**
 * 创建订单
 * @param array $data 订单数据
 * @return array 订单信息
 * @event OrderCreateAfterListener 订单创建后触发，传递参数：[$order, $group, $uid, $key, $combinationId, $seckillId, $bargainId]
 */
public function createOrder(array $data)
{
    // 订单创建逻辑
    
    // 触发事件
    event('OrderCreateAfterListener', [$order, $group, $uid, $key, $combinationId, $seckillId, $bargainId]);
    
    return $order;
}
```

## 10. 输入输出示例

### 10.1 触发订单创建事件

**输入**：
```php
// 订单创建完成后触发事件
$order = [
    'id' => 1001,
    'order_sn' => '2023120100001',
    'uid' => 101,
    'total_price' => 99.99,
    // 其他订单信息
];
$group = [
    // 订单商品组信息
];
$uid = 101;
$key = 'order_cache_key';
$combinationId = 0;
$seckillId = 0;
$bargainId = 0;

// 触发事件
event('OrderCreateAfterListener', [$order, $group, $uid, $key, $combinationId, $seckillId, $bargainId]);
```

**输出**：
```
// 事件触发后，监听器执行以下操作：
1. 计算商品实际金额、佣金、优惠折扣
2. 设置默认地址
3. 清理购物车
4. 清除订单缓存
5. 写入订单记录表
6. 加入订单自动取消延迟队列
7. 记录下单行为
8. 触发小票打印
```

### 10.2 触发用户注册事件

**输入**：
```php
// 用户注册完成后触发事件
$spreadUid = 201; // 推荐人ID
$userType = 1; // 用户类型
$name = '张三'; // 用户名
$uid = 301; // 新用户ID
$isNew = true; // 是否新用户

// 触发事件
event('UserRegisterListener', [$spreadUid, $userType, $name, $uid, $isNew]);
```

**输出**：
```
// 事件触发后，监听器执行以下操作：
1. 邀请新用户增加推荐人经验
2. 增加推荐人推广佣金
3. 处理分销等级升级
4. 记录推广绑定关系
5. 发送下级用户绑定成功通知
6. 发送新人优惠券
7. 开启推广权限（如果开启了人人分销）
```

## 11. 总结

CRMEB 的事件系统提供了一种灵活、解耦的模块间通信机制，通过事件和监听器的配合，实现了业务逻辑的分离和扩展。合理使用事件系统可以：

1. **提高代码的可维护性**：将相关业务逻辑集中到监听器中，便于管理和维护
2. **增强系统的可扩展性**：通过事件机制，可以轻松添加新的业务逻辑，而无需修改原有代码
3. **降低模块间的耦合度**：模块间通过事件进行通信，而不是直接依赖，减少了模块间的耦合
4. **提高系统的响应速度**：对于耗时的操作，可以通过队列进行异步处理，提高系统的响应速度

在开发过程中，应遵循事件命名规范、监听器设计原则和性能优化建议，确保事件系统的高效运行。通过合理利用事件机制，可以大大提高代码质量和开发效率，为系统的长期维护和扩展奠定良好的基础。