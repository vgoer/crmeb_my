# 配置参数说明

## ⚙️ 系统核心配置

### 数据库配置
```php
// config/database.php
'connections' => [
    'mysql' => [
        'hostname' => '127.0.0.1',    // 数据库主机
        'database' => 'crmeb',         // 数据库名
        'username' => 'root',          // 用户名
        'password' => '',              // 密码
        'hostport' => '3306',          // 端口
        'charset'  => 'utf8mb4',       // 字符集
    ]
]
```

### Redis缓存配置
```php
// config/cache.php
'redis' => [
    'host'     => '127.0.0.1',    // Redis主机
    'port'     => 6379,           // Redis端口
    'password' => '',             // 密码
    'select'   => 0,              // 数据库索引
    'timeout'  => 0,              // 超时时间
]
```

### 文件存储配置
```php
// config/filesystem.php
'disks' => [
    'local' => [
        'driver' => 'local',      // 本地存储
        'root'   => app()->getRootPath() . 'public/uploads',
    ],
    'qiniu' => [                  // 七牛云存储
        'driver'     => 'qiniu',
        'access_key' => env('qiniu.access_key'),
        'secret_key' => env('qiniu.secret_key'),
        'bucket'     => env('qiniu.bucket'),
        'domain'     => env('qiniu.domain'),
    ]
]
```

## 🔧 业务相关配置

### 短信配置
```php
// 短信服务商配置
'sms' => [
    'default' => 'aliyun',        // 默认服务商
    'aliyun' => [                 // 阿里云短信
        'access_key_id' => '',
        'access_key_secret' => '',
        'sign_name' => '',        // 短信签名
    ]
]
```

### 支付配置
```php
// 微信支付配置
'wechat_pay' => [
    'app_id' => '',              // 应用ID
    'mch_id' => '',              // 商户号
    'key'    => '',              // API密钥
    'cert_path' => '',           // 证书路径
    'key_path'  => '',           // 密钥路径
]
```

### 第三方服务配置
```php
// 物流查询配置
'express' => [
    'default' => 'kuaidi100',    // 默认物流查询
    'kuaidi100' => [             // 快递100
        'customer' => '',        // 客户编号
        'key'      => '',        // 授权key
    ]
]
```

## 🌐 环境变量配置 (.env)

```ini
# 数据库配置
DATABASE_HOST=127.0.0.1
DATABASE_NAME=crmeb
DATABASE_USERNAME=root
DATABASE_PASSWORD=

# Redis配置  
REDIS_HOST=127.0.0.1
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_SELECT=0

# 应用配置
APP_DEBUG=false
APP_URL=http://localhost

# 文件上传配置
UPLOAD_TYPE=local
UPLOAD_MAXSIZE=10485760
```

---

> **提示**：该文档由AI生成，仅供参考。
