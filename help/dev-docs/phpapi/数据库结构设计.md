# 数据库结构设计

## 📊 核心数据表

### 用户相关表
```
user                    # 用户主表
user_level              # 用户等级表
user_label              # 用户标签表
user_address            # 用户地址表
user_collect            # 用户收藏表
user_cart               # 购物车表
user_balance            # 用户余额表
user_integral           # 用户积分表
```

### 商品相关表
```
store_product           # 商品主表
store_product_attr      # 商品属性表
store_product_attr_value # 商品属性值表
store_category          # 商品分类表
store_product_reply     # 商品评价表
store_product_visit     # 商品浏览记录表
store_product_cate      # 商品分类关联表
```

### 订单相关表
```
store_order             # 订单主表
store_order_cart_info   # 订单购物车信息表
store_order_status      # 订单状态记录表
store_order_refund      # 退款记录表
store_order_invoice     # 发票信息表
store_pay               # 支付记录表
```

### 营销相关表
```
store_coupon            # 优惠券表
store_coupon_user       # 用户优惠券表
store_combination       # 拼团活动表
store_bargain           # 砍价活动表
store_seckill           # 秒杀活动表
store_integral_goods    # 积分商品表
store_gift              # 赠品表
```

### 公众号/小程序相关表
```
wechat_user             # 微信用户表
wechat_message          # 微信消息表
wechat_qrcode           # 二维码表
wechat_template         # 模板消息表
```

### 系统配置表
```
system_config           # 系统配置表
system_admin            # 管理员表
system_role             # 角色表
system_menus            # 菜单表
system_log              # 操作日志表
```

## 🔗 表关系说明

### 订单核心关系
```
store_order (1) ──→ (N) store_order_cart_info
store_order (1) ──→ (N) store_order_status
store_order (1) ──→ (N) store_pay
store_order (1) ──→ (N) store_order_refund
```

### 用户与订单关系
```
user (1) ──→ (N) store_order
user (1) ──→ (N) user_address
user (1) ──→ (N) user_cart
user (1) ──→ (N) store_coupon_user
```

### 商品核心关系
```
store_product (1) ──→ (N) store_product_attr
store_product (1) ──→ (N) store_product_attr_value
store_product (1) ──→ (N) store_category
store_product (1) ──→ (N) store_product_reply
```

## 📝 索引设计

### 主键索引
所有表均以 `id` 字段作为主键自增索引

### 常用查询索引
```
user表: phone, openid, unionid
store_order表: order_id, uid, status, add_time
store_product表: cate_id, is_hot, is_new, is_benefit
wechat_user表: openid, uid
```

### 联合索引
```sql
-- 订单列表查询
INDEX idx_order_list (uid, add_time, status)

-- 商品列表查询  
INDEX idx_product_list (cate_id, is_hot, is_new)
```

---

> **提示**：该文档由AI生成，仅供参考。
